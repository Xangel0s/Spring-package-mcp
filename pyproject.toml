[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "spring-package-mcp"
version = "0.1.0"
description = "Servidor MCP inteligente para gestionar dependencias Spring Boot"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
